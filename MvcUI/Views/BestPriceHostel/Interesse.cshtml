@model Mercoplano.Simplex.Server.MvcUI.Entity.Interesse
@using Mercoplano.Simplex.Server.MvcUI.Controllers;


@{
    ViewBag.Title = BaseMvc.GetLabel("BestPriceHostel", ViewBag.LanguageId);
    Layout = "~/Views/Shared/_LayoutPageBestPrice.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Interesse</h4>
        <hr />
        @Html.ValidationSummary(true)
        <div class="form-group">
            <div class="col-md-12">
                @Html.Display("O que você procura?")
            </div>
            <div class="col-md-10">
                @Html.EditorFor(model => model.Interesse1)
                @Html.ValidationMessageFor(model => model.Interesse1)
            </div>
        </div>


        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Procurar" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
